import { Stack } from "expo-router";

export default function DayOutfitsLayout() {
  return (
    <Stack
      screenOptions={{
        headerShown: true,
        title: "Day outfits",
      }}
    />
  );
}
